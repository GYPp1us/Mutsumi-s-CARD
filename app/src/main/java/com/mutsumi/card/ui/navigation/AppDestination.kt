package com.mutsumi.card.ui.navigation

enum class AppDestination(
    val label: String,
    val glyph: String,
) {
    Study("学习", "学"),
    Cards("卡片", "卡"),
    Draw("绘制", "笔"),
    Backup("备份", "备"),
}

